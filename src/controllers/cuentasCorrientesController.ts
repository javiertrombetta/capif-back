import { Request, Response } from 'express';

export const getCuentaCorriente = (req: Request, res: Response): void => {
    
};

export const getCuentaCorrienteById = (req: Request, res: Response): void => {
    
};