import { Request, Response } from 'express';

export const getConflictos = (req: Request, res: Response): void => {
    
};

export const createConflicto = (req: Request, res: Response): void => {
    
};

export const updateConflicto = (req: Request, res: Response): void => {
    
};

export const getConflictoById = (req: Request, res: Response): void => {
    
};

export const addComentarioConflicto = (req: Request, res: Response): void => {
    
};