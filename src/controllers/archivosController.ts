import { Request, Response } from 'express';

export const getArchivos = (req: Request, res: Response): void => {
    
};

export const uploadArchivo = (req: Request, res: Response): void => {
    
};

export const getArchivoById = (req: Request, res: Response): void => {
    
};

export const deleteArchivo = (req: Request, res: Response): void => {
    
};

