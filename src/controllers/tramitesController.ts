import { Request, Response } from 'express';

export const getTramites = (req: Request, res: Response): void => {
    
};

export const createTramite = (req: Request, res: Response): void => {
    
};

export const updateTramite = (req: Request, res: Response): void => {
    
};

export const deleteTramite = (req: Request, res: Response): void => {
    
};