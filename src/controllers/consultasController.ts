import { Request, Response } from 'express';

export const createConsulta = (req: Request, res: Response): void => {
    
};

export const getDatosPersonales = (req: Request, res: Response): void => {
    
};