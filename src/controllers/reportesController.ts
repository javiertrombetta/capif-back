import { Request, Response } from 'express';

export const getReportes = (req: Request, res: Response): void => {
    
};

export const generateISRCReport = (req: Request, res: Response): void => {
    
};

export const getReporteById = (req: Request, res: Response): void => {
    
};

export const downloadReporte = (req: Request, res: Response): void => {
    
};